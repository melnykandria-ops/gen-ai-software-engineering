# 📸 Screenshots

This folder holds the visual evidence required by the assignment:

1. **`ai-*.png`** — Claude Code interactions: the prompts given to the AI assistant
   and the code it generated. (Screenshots of the AI-assisted development session.)
2. **`api-running.png`** — the API server running (startup log / health endpoint).
3. **`api-list.png`** — `GET /transactions` response in the browser (list of transactions).
4. **`api-summary.png`** — `GET /accounts/ACC-12345/summary` response.
5. **`api-validation-400.png`** — a validation error response (`400`).

> A plain-text capture of the real endpoint output is also saved at
> [`../api-verification-log.txt`](../api-verification-log.txt).
